public class WeekCChallenge {

	public static void main(String[] args){

		//1. create a new instance of the Calculator class
		Calculator c = new Calculator();

		//2. call each of Calculator's instance methods: add, subtract, multiply, and divide
		// with arguments of your choice, assigning the result to the variables below


		double additionResult = c.add(20,10);
		double subtractionResult = c.subtract(20,10);
		double multiplicationResult = c.multiply(20,10);
		double divisionResult = c.divide(20,10);

		//3. print the value of each of these variables to the console with a descriptive message
		System.out.println("The answer to this addition problem is " + additionResult);
		System.out.println("The answer to this subtraction problem is " + subtractionResult);
		System.out.println("The answer to this multiplication problem is " + multiplicationResult);
		System.out.println("The answer to this division problem is " + divisionResult);

		//4. implement the static fizzBuzz method in this class and call it from the main method
	}
	

	public static void fizzBuzz(int m, int n, int[] numbers, String[] terms){
			//int result = 0;
		//	if (m == 0) {
		//	String stringOutput =  Integer.toString(m);
		//	System.out.println("error"); }
			//if (i=m; i <= m; i++) {
			//	for (int j in numbers) {
				//	System.out.println(j)
				//	}
			//	)
	//	int[] array1 = {1,2,3};
	//	String[] array2 = {"red","green","blue"};
          //      String resultArray[] = Integer.toString((fizzBuzz(0, 10, array1, array2));
	//	System.out.println(resultArray);
	} 
}







